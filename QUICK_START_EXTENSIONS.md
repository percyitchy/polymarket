# 🚀 Быстрый старт с установленными расширениями

## ✅ Установленные расширения

- ✅ REST Client (humao)
- ✅ Playwright Test (Microsoft)
- ✅ SQLite (Alexey Kozlov) - бесплатный
- ✅ GraphQL (GraphQL Foundation)

---

## 1️⃣ REST Client - Тестирование API

### Как использовать

1. **Откройте файл `api-tests.http`** в корне проекта
2. **Настройте переменные** в `.vscode/settings.json`:

   ```json
   "rest-client.environmentVariables": {
     "local": {
       "HASHDIVE_API_KEY": "ваш-ключ-здесь",
       "WALLET_ADDRESS": "0x123...",
       "CONDITION_ID": "0x456..."
     }
   }
   ```

3. **Выберите окружение** (local/production) в правом нижнем углу VS Code
4. **Нажмите "Send Request"** над любым запросом в `api-tests.http`

### Примеры использования

```http
### Тест Gamma API
GET https://gamma-api.polymarket.com/events

### Тест HashDive API (нужен API ключ)
GET https://hashdive.com/api/get_api_usage
x-api-key: {{HASHDIVE_API_KEY}}
```

### Полезные команды

- `Ctrl+Alt+R` (Cmd+Alt+R на Mac) - выполнить запрос
- `Ctrl+Alt+E` (Cmd+Alt+E на Mac) - выполнить все запросы в файле
- `Ctrl+Alt+H` (Cmd+Alt+H на Mac) - показать историю запросов

---

## 2️⃣ SQLite - Работа с базой данных (бесплатный)

### Как использовать

1. **Откройте базу данных**:
   - Нажмите `F1` или `Cmd+Shift+P`
   - Введите "SQLite: Open Database"
   - Выберите `polymarket_notifier.db`

   **Примечание**: Если используете расширение `alexcvzz.vscode-sqlite`, команда будет "SQLite: Open Database"

2. **Просмотр таблиц**:
   - В боковой панели появится раздел "SQLITE EXPLORER"
   - Разверните `polymarket_notifier.db`
   - Вы увидите все таблицы: `wallets`, `alerts_sent`, `trades`, и т.д.

3. **Выполнение SQL запросов**:
   - Откройте таблицу двойным кликом
   - Или создайте новый SQL файл (`.sql`)
   - Напишите запрос и выполните его

### Полезные SQL запросы

```sql
-- Просмотр всех кошельков
SELECT * FROM wallets LIMIT 10;

-- Статистика по кошелькам
SELECT 
    source,
    COUNT(*) as count,
    AVG(win_rate) as avg_win_rate,
    AVG(trades) as avg_trades
FROM wallets
GROUP BY source;

-- Последние алерты
SELECT * FROM alerts_sent 
ORDER BY sent_at DESC 
LIMIT 20;

-- Кошельки с лучшим win rate
SELECT address, win_rate, trades, source
FROM wallets
WHERE trades >= 10
ORDER BY win_rate DESC
LIMIT 10;
```

---

## 3️⃣ Playwright Test - Отладка парсинга

### Как использовать

1. **Запуск тестов Playwright**:
   - Откройте файл с тестами (например, `test_*.py`)
   - Нажмите на иконку Playwright рядом с тестом
   - Или используйте панель "Testing" в боковой панели

2. **Отладка браузерной автоматизации**:
   - Установите breakpoint в коде парсинга
   - Запустите отладку (`F5`)
   - Playwright откроет браузер и остановится на breakpoint

3. **Просмотр трассировки**:
   - После выполнения теста появится кнопка "Show Trace"
   - Откроется визуальная трассировка всех действий браузера

### Пример отладки `fetch_leaderboards.py`

```python
# Установите breakpoint здесь:
async def scrape_leaderboard(self):
    page = await self.context.new_page()  # <- breakpoint
    await page.goto("https://polymarket.com/leaderboard")
    # ... остальной код
```

### Полезные команды

- `F5` - запустить отладку
- `F9` - установить/убрать breakpoint
- `F10` - шаг через (step over)
- `F11` - шаг внутрь (step into)

---

## 4️⃣ GraphQL - Работа с GraphQL запросами

### Как использовать

1. **Создайте файл `.graphql`**:

   ```graphql
   # queries.graphql
   query GetEvent($slug: String!) {
     event(slug: $slug) {
       id
       title
       markets {
         id
         slug
         outcomePrices
       }
     }
   }
   ```

2. **Подсветка синтаксиса**:
   - GraphQL файлы автоматически получают подсветку
   - Автодополнение для типов и полей

3. **Валидация запросов**:
   - Ошибки в GraphQL запросах будут подсвечены
   - Автодополнение полей из схемы

### Пример использования в Python

```python
# В вашем коде используйте GraphQL запросы:
query = """
query GetEvent($slug: String!) {
  event(slug: $slug) {
    id
    title
    markets {
      id
      slug
    }
  }
}
"""

# GraphQL расширение поможет с автодополнением и валидацией
```

---

## 🎯 Практические примеры для вашего проекта

### Тестирование Gamma API через REST Client

1. Откройте `api-tests.http`
2. Найдите секцию "Gamma API"
3. Нажмите "Send Request" над запросом
4. Посмотрите ответ в правой панели

### Проверка базы данных через SQLite Viewer

1. Откройте `polymarket_notifier.db`
2. Разверните таблицу `wallets`
3. Двойной клик для просмотра данных
4. Используйте SQL запросы для фильтрации

### Отладка парсинга через Playwright

1. Откройте `fetch_leaderboards.py`
2. Установите breakpoint в методе `scrape_leaderboard`
3. Запустите отладку (`F5`)
4. Браузер откроется и остановится на breakpoint
5. Проверьте переменные и выполните код пошагово

---

## 💡 Полезные советы

### REST Client

- Сохраняйте часто используемые запросы в `api-tests.http`
- Используйте переменные для разных окружений
- Просматривайте историю запросов для отладки

### SQLite Viewer

- Используйте SQL запросы вместо просмотра всей таблицы
- Экспортируйте данные в CSV для анализа
- Создавайте индексы для быстрого поиска

### Playwright Test

- Используйте `headless: false` для визуальной отладки
- Делайте скриншоты при ошибках: `await page.screenshot()`
- Используйте `page.pause()` для интерактивной отладки

### GraphQL

- Храните запросы в отдельных `.graphql` файлах
- Используйте фрагменты для переиспользования
- Валидируйте запросы перед отправкой

---

## 🚀 Следующие шаги

1. **Протестируйте REST Client**:
   - Откройте `api-tests.http`
   - Настройте переменные окружения
   - Выполните несколько запросов

2. **Изучите базу данных**:
   - Откройте `polymarket_notifier.db`
   - Просмотрите структуру таблиц
   - Выполните несколько SQL запросов

3. **Отладьте парсинг**:
   - Установите breakpoint в `fetch_leaderboards.py`
   - Запустите отладку
   - Посмотрите как работает Playwright

4. **Создайте GraphQL запросы**:
   - Создайте файл `queries.graphql`
   - Добавьте запросы для Polymarket API
   - Используйте их в коде

---

## 📚 Дополнительные ресурсы

- **REST Client**: <https://marketplace.visualstudio.com/items?itemName=humao.rest-client>
- **Playwright**: <https://playwright.dev/python/>
- **SQLite Viewer**: <https://marketplace.visualstudio.com/items?itemName=qwtel.sqlite-viewer>
- **GraphQL**: <https://graphql.org/>

---

Готово! Теперь вы можете эффективно работать с API, базой данных и парсингом прямо в VS Code! 🎉
