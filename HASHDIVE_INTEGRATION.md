# HashiDive API Integration Guide

## ✅ Что уже реализовано

### 1. **Fallback для получения trades**
- HashiDive используется как резервный источник, когда Polymarket API не отвечает
- Автоматически активируется в `get_new_trades()` если Polymarket API возвращает пустой результат
- Фильтрует только свежие сделки (последние 7 дней) чтобы избежать закрытых рынков

### 2. **Поиск кошельков китов (Whale Wallet Discovery)**
- Новая функция `collect_wallets_from_hashdive_whales()` в `polymarket_notifier.py`
- Интегрирована в `daily_wallet_analysis.py`
- Проверяет остаток кредитов перед использованием
- Извлекает уникальные адреса из крупных сделок (whale trades)

## 📋 Настройка

### Шаг 1: Добавьте API ключ в `.env`

```bash
# Откройте .env файл
nano .env

# Добавьте ваш HashiDive API ключ:
HASHDIVE_API_KEY=your_actual_api_key_here
```

### Шаг 2: Включите сбор кошельков китов (опционально)

```bash
# В .env добавьте:
ENABLE_HASHDIVE_WHALES=true
```

**Важно:** По умолчанию `ENABLE_HASHDIVE_WHALES=false`, чтобы не тратить кредиты без необходимости.

### Шаг 3: Проверьте лимит кредитов

HashiDive предоставляет **1000 кредитов в месяц** (1 кредит = 1 запрос).

Проверить использование:
```bash
python3 -c "from hashdive_client import HashDiveClient; import os; client = HashDiveClient(os.getenv('HASHDIVE_API_KEY')); print(client.get_api_usage())"
```

## 🎯 Возможности HashiDive API

### Доступные эндпоинты:

1. **`get_trades`** ✅ Уже используется
   - Fallback для получения сделок кошелька
   - Автоматически активируется при сбое Polymarket API

2. **`get_latest_whale_trades`** ✅ Интегрировано
   - Поиск кошельков китов (сделки > $5000)
   - Используется в `daily_wallet_analysis.py` если `ENABLE_HASHDIVE_WHALES=true`

3. **`get_positions`** ⚠️ Не используется (можно добавить)
   - Текущие позиции кошелька
   - Может помочь в анализе активных позиций

4. **`get_last_price`** ⚠️ Не используется (можно добавить)
   - Последняя цена токена
   - Может быть более надежным источником цен

5. **`search_markets`** ⚠️ Не используется (можно добавить)
   - Поиск активных рынков
   - Может помочь в поиске новых рынков для мониторинга

6. **`get_ohlcv`** ⚠️ Не используется (можно добавить)
   - OHLCV данные для анализа графиков
   - Может использоваться для технического анализа

## 📊 Использование кредитов

### Текущее использование:
- **Fallback trades**: ~1-2 кредита на кошелек (только при сбое Polymarket API)
- **Whale trades**: 1 кредит на вызов (если включено)

### Рекомендации:
- Оставьте `ENABLE_HASHDIVE_WHALES=false` если кредитов мало
- Используйте whale trades только для периодического поиска новых кошельков
- Fallback для trades активируется автоматически и не требует настройки

## 🔧 Примеры использования

### Тест API ключа:
```bash
python3 -c "
from hashdive_client import HashDiveClient
import os
from dotenv import load_dotenv

load_dotenv()
api_key = os.getenv('HASHDIVE_API_KEY')
if api_key:
    client = HashDiveClient(api_key)
    usage = client.get_api_usage()
    print(f'Credits: {usage.get(\"credits_used\", 0)}/{usage.get(\"credits_limit\", 1000)}')
else:
    print('HASHDIVE_API_KEY not found in .env')
"
```

### Ручной поиск китов:
```bash
python3 << 'EOF'
from hashdive_client import HashDiveClient
import os
from dotenv import load_dotenv

load_dotenv()
api_key = os.getenv('HASHDIVE_API_KEY')
if api_key:
    client = HashDiveClient(api_key)
    whales = client.get_latest_whale_trades(min_usd=5000, limit=50)
    results = whales.get('results', []) if isinstance(whales, dict) else whales
    wallets = set()
    for trade in results:
        user = trade.get('user_address') or trade.get('user')
        if user:
            wallets.add(user.lower())
    print(f'Found {len(wallets)} unique whale wallets')
    print(f'Sample: {list(wallets)[:5]}')
else:
    print('HASHDIVE_API_KEY not found')
EOF
```

## 📝 Логирование

HashiDive операции логируются с префиксом `[HashiDive]`:
- `[HashiDive] API credits: X/1000 used, Y remaining`
- `[HashiDive] Collecting whale wallets (min $5,000, limit 200)...`
- `[HashiDive] Found N whale trades`
- `[HashiDive] Extracted M unique whale wallets`

## ⚠️ Важные замечания

1. **Лимит кредитов**: 1000 кредитов/месяц. Используйте экономно.
2. **Whale trades**: Включайте только при необходимости поиска новых кошельков.
3. **Fallback trades**: Работает автоматически, не требует настройки.
4. **Проверка кредитов**: Система автоматически проверяет остаток перед использованием whale trades.

## 🚀 Следующие шаги (опционально)

Если нужно расширить использование HashiDive:

1. **Добавить `get_last_price` для проверки цен**
   - Более надежный источник цен чем Polymarket API
   - Можно использовать для валидации цен перед отправкой алертов

2. **Добавить `get_positions` для анализа активных позиций**
   - Понимание текущих позиций кошельков
   - Может помочь в фильтрации кошельков

3. **Добавить `search_markets` для поиска новых рынков**
   - Автоматический поиск активных рынков
   - Расширение покрытия мониторинга

