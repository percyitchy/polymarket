# 📊 Обновление базы данных кошельков - Полное описание

## 🔄 Источники данных

Наша база данных кошельков обновляется из **3 основных источников**:

### 1. **polymarketanalytics.com/traders** (Основной источник)
- **API Endpoint**: `https://polymarketanalytics.com/api/traders-tag-performance`
- **Метод**: POST запрос с пагинацией
- **Объем**: До **2000 адресов** за раз
- **Частота**: **Каждые 12 часов** (автоматически через cron)
- **Скрипт**: `check_polymarket_analytics.py`

### 2. **Polymarket Leaderboards** (Дополнительный источник)
- **Источники**:
  - Weekly profit leaderboard
  - Monthly profit leaderboard
- **Метод**: API через `api_scraper.py` и Playwright scraping
- **Частота**: По требованию / при запуске `refresh_wallets_from_leaderboards.py`

### 3. **Ручной сбор** (Batch режим)
- **Скрипт**: `batch_analyze_wallets.py`
- **Использование**: Массовый анализ кошельков по требованию

---

## ⚙️ Процесс обновления

### Этап 1: Сбор адресов (Collection)

#### A. Автоматическая проверка polymarketanalytics.com (каждые 12 часов)

```bash
# Cron job (на сервере)
0 */12 * * * cd /path/to/polymarket && python3 check_polymarket_analytics.py
```

**Процесс:**
1. Скрипт делает POST запрос к API `polymarketanalytics.com/api/traders-tag-performance`
2. Получает до 2000 адресов трейдеров
3. Проверяет каждый адрес:
   - ❌ Пропускает, если уже есть в таблице `wallets`
   - ❌ Пропускает, если уже в очереди `wallet_analysis_jobs`
   - ✅ Добавляет в очередь `wallet_analysis_jobs`, если новый

#### B. Обновление из Leaderboards (по требованию)

```bash
python3 refresh_wallets_from_leaderboards.py
```

**Процесс:**
1. Сканирует weekly и monthly leaderboards через Polymarket API
2. Собирает адреса из топ-трейдеров
3. Добавляет новые адреса в очередь анализа

---

### Этап 2: Очередь анализа (Queue System)

Все новые адреса попадают в таблицу **`wallet_analysis_jobs`**:

```sql
CREATE TABLE wallet_analysis_jobs (
    id INTEGER PRIMARY KEY,
    address TEXT,
    display TEXT,
    source TEXT,
    status TEXT,  -- pending/processing/completed/failed
    created_at TEXT,
    updated_at TEXT,
    next_retry_at TEXT
)
```

**Статусы:**
- `pending` - Ожидает обработки
- `processing` - Обрабатывается воркером
- `completed` - Анализ завершен
- `failed` - Ошибка (будет повторно попробовано)

---

### Этап 3: Анализ кошельков (Worker Processing)

**Воркеры**: 7 параллельных воркеров (`api_max_workers = 7`)

#### Быстрый фильтр (Fast Filter):
```
Если traded < 6 → ❌ Сразу отклоняется без API запросов
```

#### Полный анализ для кошельков с ≥6 trades:

1. **API запрос 1**: `/traded`
   - Получает `traded` (количество закрытых позиций)
   - Если `traded < 6` → отклоняется

2. **API запрос 2**: `/closed-positions`
   - Получает историю закрытых позиций
   - Рассчитывает:
     - ✅ **Win Rate**: Процент выигрышных рынков
     - ✅ **Total PnL**: Суммарный профит/убыток
     - ✅ **Daily Frequency**: Средняя частота торговли (позиций/день)

3. **Проверка критериев**:
   ```
   ✅ traded >= 6
   ✅ win_rate >= 0.75 (75%)
   ✅ win_rate <= 1.0 (100%)
   ✅ traded <= 1000
   ✅ daily_trading_frequency <= 20.0 сделок/день
   ```

4. **Результат**:
   - ✅ **Проходит** → Добавляется в таблицу `wallets`
   - ❌ **Не проходит** → Сохраняется в кэш `wallet_analysis_cache` (TTL 3 часа)

---

### Этап 4: Сохранение в базу данных

**Таблица `wallets`**:
```sql
CREATE TABLE wallets (
    address TEXT PRIMARY KEY,
    display TEXT,
    traded_total INTEGER,        -- Всего закрытых позиций
    win_rate REAL,               -- Win Rate (0.75 - 1.0)
    realized_pnl_total REAL,     -- Суммарный PnL
    daily_trading_frequency REAL, -- Сделок/день
    source TEXT,                 -- Источник (polymarket_analytics_periodic, etc.)
    added_at TEXT,
    updated_at TEXT
)
```

---

## 🧹 Очистка и поддержка базы

### Ежедневная очистка (Daily Pruning)

**Скрипт**: `export_wallets_to_excel.py` (запускается ежедневно)

**Процесс:**
1. Удаляет кошельки, которые больше не соответствуют критериям:
   ```sql
   DELETE FROM wallets
   WHERE traded_total < 6
      OR traded_total > 1000
      OR win_rate < 0.75
      OR win_rate > 1.0
      OR daily_trading_frequency > 20.0
   ```

2. Экспортирует текущие кошельки в Excel файл

**Cron job:**
```bash
# Запускается ежедневно (настроить время в cron)
0 3 * * * cd /path/to/polymarket && python3 export_wallets_to_excel.py
```

---

## 📈 Мониторинг и отслеживание

### Система кэширования

**Таблица `wallet_analysis_cache`**:
- Хранит результаты анализа на 3 часа (TTL)
- Предотвращает повторные API запросы для недавно проверенных кошельков

### Логирование

**Файлы логов:**
- `refresh_wallets.log` - Обновление из leaderboards
- `batch_analyze_wallets.log` - Batch анализ
- `polymarket_notifier.log` - Основной лог бота

---

## 🎯 Критерии отбора (финальные)

| Параметр | Значение | Описание |
|----------|----------|----------|
| **Минимум сделок** | **≥6** | Минимум 6 закрытых позиций |
| **Win Rate** | **75% - 100%** | Процент выигрышных рынков |
| **Максимум сделок** | **≤1000** | Исключает ботов/масс-трейдеров |
| **Дневная активность** | **≤20 сделок/день** | Средняя частота торговли |
| **MAX_WALLETS** | **2000** | Максимум отслеживаемых одновременно |

---

## 🔄 График автоматического обновления

| Задача | Частота | Скрипт | Описание |
|--------|---------|--------|----------|
| Проверка polymarketanalytics.com | Каждые 12 часов | `check_polymarket_analytics.py` | Добавление новых адресов в очередь |
| Очистка базы | Ежедневно (03:00) | `export_wallets_to_excel.py` | Удаление несоответствующих критериям |
| Обработка очереди | Постоянно | `wallet_analyzer.py` (воркеры) | 7 воркеров обрабатывают очередь 24/7 |

---

## 📊 Статистика источников

По состоянию на 2 ноября 2025:

- **polymarket_analytics_periodic**: ~1320 кошельков (основной источник)
- **polymarket_leaderboard**: ~300-400 кошельков (дополнительный)
- **polymarket_api**: ~100-200 кошельков (через официальный API)

**Всего в базе**: ~1500-2000 кошельков, соответствующих критериям

---

## 🚀 Ручной запуск

### Проверить новые кошельки из polymarketanalytics.com:
```bash
python3 check_polymarket_analytics.py
```

### Обновить из leaderboards:
```bash
python3 refresh_wallets_from_leaderboards.py
```

### Пакетный анализ (1200 кошельков):
```bash
python3 batch_analyze_wallets.py
```

### Проверить статус очереди:
```python
from db import PolymarketDB
from wallet_analyzer import WalletAnalyzer, AnalysisConfig

db = PolymarketDB("polymarket_notifier.db")
analyzer = WalletAnalyzer(db, AnalysisConfig())
status = analyzer.get_queue_status()
print(status)
```

---

## ⚠️ Важные замечания

1. **Rate Limiting**: API запросы делаются с задержками, чтобы не превысить лимиты
2. **Retry Logic**: Ошибки API автоматически повторяются с экспоненциальной задержкой
3. **Fast Filter**: Кошельки с <6 trades сразу отклоняются без API запросов (экономия ресурсов)
4. **TTL Cache**: Результаты анализа кэшируются на 3 часа для оптимизации

---

*Документация обновлена: 2 ноября 2025*

