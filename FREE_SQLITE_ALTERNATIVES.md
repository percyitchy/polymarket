# 🆓 Бесплатные альтернативы SQLite Viewer

## ✅ Рекомендуемые бесплатные расширения

### 1. **SQLite** (Alexey Kozlov) - РЕКОМЕНДУЕТСЯ
- **ID**: `alexcvzz.vscode-sqlite`
- **Статус**: ✅ Полностью бесплатный
- **Функции**:
  - Просмотр таблиц SQLite
  - Выполнение SQL запросов
  - Просмотр структуры базы данных
  - Экспорт данных

**Как использовать:**
1. Установите расширение: `alexcvzz.vscode-sqlite`
2. Нажмите `F1` → "SQLite: Open Database"
3. Выберите `polymarket_notifier.db`
4. В боковой панели появится "SQLITE" с таблицами

### 2. **SQLite Viewer** (Florian Klampfer)
- **ID**: `qwtel.sqlite-viewer`
- **Статус**: ⚠️ Имеет платные функции, но базовая версия бесплатна
- **Функции**:
  - Базовый просмотр таблиц (бесплатно)
  - Расширенные функции (платно)

### 3. **Database Client JDBC** (cweijan)
- **ID**: `cweijan.vscode-database-client2`
- **Статус**: ✅ Бесплатный
- **Функции**:
  - Поддержка SQLite, MySQL, PostgreSQL и др.
  - Визуальный редактор таблиц
  - Выполнение SQL запросов
  - Экспорт данных

---

## 🎯 Рекомендация: Используйте **SQLite** (alexcvzz)

Это расширение полностью бесплатное и имеет все необходимые функции для работы с SQLite базами данных.

### Быстрая установка:

1. Откройте VS Code Marketplace
2. Найдите: `SQLite` от **Alexey Kozlov**
3. Или установите через команду:
   ```
   code --install-extension alexcvzz.vscode-sqlite
   ```

### Использование:

```bash
# Открыть базу данных
F1 → "SQLite: Open Database" → выберите polymarket_notifier.db

# Выполнить SQL запрос
# Создайте файл .sql и напишите запрос:
SELECT * FROM wallets LIMIT 10;
```

---

## 📝 Альтернативный способ без расширений

Если не хотите устанавливать расширения, можете использовать:

### 1. **Python скрипт для просмотра БД**:

```python
import sqlite3
import pandas as pd

# Подключение к базе
conn = sqlite3.connect('polymarket_notifier.db')

# Просмотр таблиц
tables = conn.execute("SELECT name FROM sqlite_master WHERE type='table'").fetchall()
print("Таблицы:", tables)

# Просмотр данных
df = pd.read_sql_query("SELECT * FROM wallets LIMIT 10", conn)
print(df)

conn.close()
```

### 2. **Командная строка SQLite**:

```bash
# Установка SQLite CLI (если не установлен)
# macOS: brew install sqlite
# Ubuntu: sudo apt-get install sqlite3

# Использование
sqlite3 polymarket_notifier.db

# В SQLite CLI:
.tables                    # Показать все таблицы
.schema wallets           # Показать структуру таблицы wallets
SELECT * FROM wallets LIMIT 10;  # Выполнить запрос
.quit                     # Выход
```

### 3. **DB Browser for SQLite** (отдельное приложение):

- **Скачать**: https://sqlitebrowser.org/
- **Статус**: ✅ Полностью бесплатный
- **Функции**: Визуальный редактор, SQL запросы, экспорт данных

---

## 🔄 Обновление extensions.json

Я уже обновил файл `.vscode/extensions.json` с бесплатным расширением `alexcvzz.vscode-sqlite`.

Теперь VS Code будет рекомендовать правильное бесплатное расширение!

---

## 💡 Итоговая рекомендация

**Установите**: `alexcvzz.vscode-sqlite` - это полностью бесплатное и функциональное расширение для работы с SQLite.

**Альтернатива**: Используйте DB Browser for SQLite как отдельное приложение, если предпочитаете не устанавливать расширения в VS Code.

