# Polymarket API Keys Integration

## 📋 Что дают API ключи Polymarket?

### Преимущества:

1. **🔓 Более высокие rate limits**
   - Публичные API имеют ограничения (429 errors)
   - С аутентификацией лимиты значительно выше

2. **📊 Доступ к расширенным данным**
   - Более детальная информация о маркетах
   - История сделок с большей глубиной
   - Возможно, данные о позициях кошельков

3. **⚡ Стабильность запросов**
   - Меньше блокировок по IP
   - Приоритет при обработке запросов

4. **🔐 Приватные эндпоинты**
   - Некоторые эндпоинты доступны только с аутентификацией

---

## 🔧 Как использовать:

### 1. Добавьте ключи в `.env` файл:

```bash
POLYMARKET_API_KEY=your_api_key_here
POLYMARKET_SECRET=your_secret_here
POLYMARKET_PASSPHRASE=your_passphrase_here
```

### 2. Где получить ключи:

- Зайдите на https://polymarket.com/settings/api-keys
- Создайте новые API ключи
- **ВАЖНО:** Сохраните ключи сразу - они показываются только один раз!

### 3. Бот автоматически использует ключи:

- Если ключи указаны в `.env` - бот будет использовать их для всех запросов
- Если ключи не указаны - бот работает как раньше (публичные API)

---

## 🛡️ Безопасность:

- ⚠️ **НЕ** коммитьте `.env` файл в git (уже в `.gitignore`)
- ⚠️ **НЕ** делитесь ключами публично
- ⚠️ Если ключи скомпрометированы - отзовите их и создайте новые

---

## 📝 Текущая реализация:

- Модуль `polymarket_auth.py` обрабатывает аутентификацию
- Использует HMAC SHA256 подпись (стандарт для Polymarket)
- Автоматически добавляет заголовки к запросам
- Работает прозрачно - код не требует изменений

---

## 🔍 Как проверить работу:

1. Добавьте ключи в `.env`
2. Перезапустите бот
3. В логах должно появиться: `✅ Polymarket API authentication enabled`
4. Проверьте, что запросы не получают 429 errors

---

## ⚙️ Технические детали:

Аутентификация работает через:
- **Timestamp** - текущее время
- **Signature** - HMAC-SHA256 подпись (timestamp + method + path + body)
- **Headers**: `POLY_API_KEY`, `POLY_SIGNATURE`, `POLY_PASSPHRASE`, `POLY_TIMESTAMP`

Альтернативные имена заголовков также поддерживаются (для совместимости с разными версиями API).

