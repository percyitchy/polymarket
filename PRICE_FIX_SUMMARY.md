# 🔧 Исправление проблемы с ценами в алертах

## 📋 Проблема

Во всех новых сигналах `current_price` не попадал в сообщение, и все алерты уходили в категорию:
```
🚫 Alert Suppressed: ❓ Цена недоступна
```

Вместо обычного алерта с `Price: N/A` (unavailable), хотя была внедрена логика fail-open.

## 🔍 Причины проблемы

### 1. **`price_fetcher.py` не использовал `wallet_prices` как fallback**
   - Функция `get_current_price()` не принимала параметр `wallet_prices`
   - Даже при наличии цен из сделок кошельков (например, $0.760 и $0.750), система не использовала их как fallback
   - Это приводило к тому, что при недоступности всех API цена всегда возвращалась как `None`

### 2. **Недостаточное логирование**
   - Не было видно, какие источники пробовались и почему они отказали
   - Логировались только ошибки на уровне DEBUG, что затрудняло диагностику

### 3. **Неправильная логика в `polymarket_notifier.py`**
   - При `current_price=None` и неактивном рынке отправлялся suppressed alert
   - Но если рынок был активен, код все равно мог заблокировать алерт из-за неправильной структуры условий
   - Строка 1803 делала `return` даже когда рынок был активен, что блокировало отправку алерта

### 4. **Логика fail-open не работала полностью**
   - Хотя в `notify.py` была логика fail-open (строки 415-419), она не всегда срабатывала из-за проблем в `polymarket_notifier.py`
   - Suppressed alert отправлялся даже для активных рынков, если цена была недоступна

## ✅ Исправления

### 1. **Добавлен `wallet_prices` fallback в `price_fetcher.py`**

**Файл**: `price_fetcher.py`

**Изменения**:
- Добавлен параметр `wallet_prices: Optional[Dict[str, float]] = None` в функцию `get_current_price()`
- Добавлен шаг 5: использование средней цены из `wallet_prices` как последний fallback
- Если все API недоступны, но есть `wallet_prices`, вычисляется средняя цена из них

**Код**:
```python
# Шаг 5: Средняя цена из wallet_prices (если предоставлена)
if wallet_prices:
    logger.info(f"[Price] [5/5] Trying wallet_prices fallback (provided {len(wallet_prices)} wallet prices)...")
    try:
        prices = [p for p in wallet_prices.values() if isinstance(p, (int, float)) and p > 0]
        if prices:
            avg_price = sum(prices) / len(prices)
            logger.info(f"[Price] ✅ SUCCESS: Got average price from wallet_prices: {avg_price:.6f} (from {len(prices)} wallets)")
            return avg_price
    except Exception as e:
        logger.debug(f"[Price] [5/5] Failed to calculate average from wallet_prices: {e}")
```

### 2. **Добавлено подробное логирование**

**Файлы**: `price_fetcher.py`, `notify.py`, `polymarket_notifier.py`

**Изменения**:
- Все источники цен теперь логируются с уровнем INFO
- Показывается номер шага (1/5, 2/5, и т.д.)
- Логируются статус ответа, ошибки аутентификации (401, 403), таймауты
- Логируется, почему именно источник не сработал

**Пример логов**:
```
[Price] 🔍 Starting price lookup for token_id=0x319a2f28b0de6794f3f951b7f4604d63caaeefe91a01659305189c3bc40a61be:0...
[Price] [1/5] Trying Polymarket CLOB API /price endpoint...
[Price] [1/5] Response status: 200
[Price] [1/5] ✅ SUCCESS: Got price from Polymarket CLOB API /price: 0.657000
```

### 3. **Исправлена логика fail-open в `polymarket_notifier.py`**

**Файл**: `polymarket_notifier.py`

**Изменения**:
- Исправлена структура условий при `current_price=None`
- Если рынок активен (`market_closed=False`), алерт продолжается даже при `current_price=None`
- Удален лишний `return` на строке 1803, который блокировал отправку алерта для активных рынков
- Добавлено логирование о том, что цена недоступна, но рынок активен, поэтому алерт продолжается

**Код**:
```python
else:
    # Price is None but market appears ACTIVE - allow alert to proceed (fail-open)
    logger.warning(
        f"[CONSENSUS] ⚠️  Price unavailable but market is ACTIVE - allowing alert to proceed "
        f"condition={condition_id[:20]}... outcome={outcome_index} side={side} "
        f"wallets={len(wallets_in_window)} "
        f"(price might be temporarily unavailable, but market is still active - fail-open)"
    )
    # Continue to send real alert (price will be shown as "N/A" or similar)
    # current_price remains None, but alert will be sent with Price: N/A
```

### 4. **Улучшена передача `wallet_prices` в `price_fetcher`**

**Файлы**: `notify.py`, `polymarket_notifier.py`

**Изменения**:
- `wallet_prices` теперь передается в `price_fetcher.get_current_price()` через параметр
- Добавлено логирование о количестве переданных `wallet_prices`
- Логируется результат получения цены

**Код**:
```python
logger.info(f"[Price] Trying price_fetcher module with wallet_prices fallback (provided {len(wallet_prices) if wallet_prices else 0} wallets)")
price = fetch_price(condition_id=condition_id, outcome_index=outcome_index, wallet_prices=wallet_prices)
```

### 5. **Улучшена обработка ошибок в `price_fetcher.py`**

**Файл**: `price_fetcher.py`

**Изменения**:
- Ошибки аутентификации (401, 403) логируются отдельно
- Таймауты логируются с указанием времени ожидания
- Все ошибки логируются на уровне WARNING для лучшей видимости

## 📊 Порядок источников цен (обновлен)

Теперь порядок следующий:

1. **Polymarket CLOB API /price** (с авторизацией)
2. **HashiDive API**
3. **История сделок** (data-api + clob)
4. **FinFeed API**
5. **Средняя цена из wallet_prices** ← **НОВЫЙ FALLBACK**

## 🎯 Результат

### До исправления:
- `current_price` всегда был `None` при недоступности API
- Все алерты уходили в категорию "Alert Suppressed: ❓ Цена недоступна"
- `wallet_prices` не использовались как fallback

### После исправления:
- `current_price` получается хотя бы из одного источника (включая `wallet_prices`)
- При недоступности всех API, но наличии `wallet_prices`, используется средняя цена из них
- Если все источники недоступны, но рынок активен, отправляется обычный алерт с `Price: N/A (unavailable)`
- Suppressed alert отправляется только для закрытых/разрешенных рынков, дубликатов, cooldown и т.п.
- Подробное логирование позволяет видеть, какие источники пробовались и почему они отказали

## 📁 Измененные файлы

1. **`price_fetcher.py`**
   - Добавлен параметр `wallet_prices` в `get_current_price()`
   - Добавлен шаг 5: fallback на среднюю цену из `wallet_prices`
   - Улучшено логирование всех источников (INFO/WARNING вместо DEBUG)

2. **`notify.py`**
   - Передача `wallet_prices` в `price_fetcher.get_current_price()`
   - Добавлено логирование процесса получения цены
   - Логика fail-open уже была правильной, но теперь работает корректно

3. **`polymarket_notifier.py`**
   - Передача `wallet_prices` в `price_fetcher.get_current_price()`
   - Исправлена логика fail-open: при активном рынке алерт продолжается даже при `current_price=None`
   - Добавлено подробное логирование

4. **`test_price_lookup.py`** (новый файл)
   - Тестовый скрипт для проверки получения цены для конкретного маркета

## 🔍 Проверка конкретного кейса

Для маркета Lakers vs. Hornets (condition_id: `0x319a2f28b0de6794f3f951b7f4604d63caaeefe91a01659305189c3bc40a61be`):

- **Без wallet_prices**: цена получается из истории сделок (0.657)
- **С wallet_prices**: цена получается из истории сделок (0.657), но если бы все API были недоступны, использовалась бы средняя из wallet_prices (0.755)

## 📝 Важные замечания

1. **Suppressed alerts** теперь отправляются только для реальных причин:
   - Рынок закрыт/разрешен
   - Дубликат алерта
   - Cooldown период
   - Конфликт с противоположной стороной
   - Ошибка парсинга цены

2. **Отсутствие цены** само по себе НЕ является причиной для suppressed alert:
   - Если рынок активен, но цена недоступна → отправляется обычный алерт с `Price: N/A (unavailable)`
   - Если рынок закрыт и цена недоступна → отправляется suppressed alert с reason="market_closed"

3. **wallet_prices fallback** работает только если:
   - Все API источники недоступны
   - `wallet_prices` предоставлен и содержит валидные цены (> 0)

## ✅ Итог

Проблема решена:
- ✅ `wallet_prices` используется как fallback
- ✅ Подробное логирование добавлено
- ✅ Логика fail-open исправлена
- ✅ Suppressed alerts отправляются только по реальным причинам
- ✅ Обычные алерты отправляются с `Price: N/A` при недоступности цены, но активном рынке

