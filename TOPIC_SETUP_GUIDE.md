# 📋 Настройка тем для разделения сигналов по размеру

## 🎯 Цель

Разделить сигналы на два типа:
- **Low Size**: когда общий размер позиции трейдеров < $10,000
- **High Size**: когда общий размер позиции трейдеров >= $10,000

Сигналы будут автоматически отправляться в соответствующие темы канала "POLY DAO TEST".

## 📝 Шаги настройки

### 1. Убедитесь, что канал настроен

- Канал "POLY DAO TEST" создан и является форум-группой (Topics enabled)
- Бот является администратором канала
- Созданы две темы:
  - "Low Size Alerts"
  - "High Size Alerts"

### 2. Получите ID тем

**Способ 1: Через скрипт get_topic_ids_from_updates.py (рекомендуется)**

1. Откройте канал "POLY DAO TEST" в Telegram
2. Откройте тему "Low Size Alerts"
3. Отправьте боту любое сообщение в этой теме (например, "test")
4. Откройте тему "High Size Alerts"
5. Отправьте боту любое сообщение в этой теме (например, "test")
6. Запустите скрипт:
   ```bash
   python3 get_topic_ids_from_updates.py
   ```
7. Скрипт покажет найденные Topic ID

**Способ 2: Через @RawDataBot**

1. Добавьте @RawDataBot в канал "POLY DAO TEST"
2. Откройте тему "Low Size Alerts"
3. Отправьте любое сообщение в теме
4. @RawDataBot покажет `message_thread_id` в ответе - это и есть Topic ID
5. Повторите для темы "High Size Alerts"

**Способ 3: Через веб-интерфейс Telegram**

1. Откройте https://web.telegram.org
2. Откройте канал "POLY DAO TEST"
3. Откройте тему и посмотрите в URL: `.../topic/12345`
4. Где `12345` - это Topic ID

### 3. Настройте переменные окружения

Откройте файл `.env` и добавьте/обновите следующие строки:

```env
# ID канала "POLY DAO TEST"
TELEGRAM_REPORTS_CHAT_ID=-100XXXXXXXXXX

# ID тем для разделения сигналов по размеру
TELEGRAM_LOW_SIZE_TOPIC_ID=12345   # ID темы "Low Size Alerts"
TELEGRAM_HIGH_SIZE_TOPIC_ID=67890  # ID темы "High Size Alerts"

# Порог для разделения (по умолчанию $10,000)
SIZE_THRESHOLD_USD=10000
```

### 4. Проверьте настройки

Запустите тестовый скрипт для проверки:

```bash
python3 send_test_alert.py
```

Или проверьте логи при запуске основного бота:

```bash
python3 polymarket_notifier.py
```

В логах вы увидите:
```
[NOTIFY] Sending Low Size alert to chat_id=..., topic_id=12345, total_usd=$5000.00
[NOTIFY] Sending High Size alert to chat_id=..., topic_id=67890, total_usd=$15000.00
```

## 🔧 Как это работает

1. При обнаружении консенсуса трейдеров система вычисляет `total_usd` - общий размер позиции всех трейдеров
2. Если `total_usd < SIZE_THRESHOLD_USD` (по умолчанию $10,000):
   - Сигнал отправляется в тему "Low Size Alerts" (`TELEGRAM_LOW_SIZE_TOPIC_ID`)
3. Если `total_usd >= SIZE_THRESHOLD_USD`:
   - Сигнал отправляется в тему "High Size Alerts" (`TELEGRAM_HIGH_SIZE_TOPIC_ID`)

## ⚠️ Важные замечания

- Если `total_usd` не указан или равен `None`, система использует старую логику (если настроен `TELEGRAM_TOPIC_ID`)
- Если Topic ID не настроен для соответствующей категории, сообщение будет отправлено в канал без указания темы
- Порог `SIZE_THRESHOLD_USD` можно изменить в `.env` файле

## 🐛 Устранение проблем

**Проблема**: Сигналы не приходят в темы

**Решение**:
1. Проверьте, что Topic ID указаны правильно в `.env`
2. Убедитесь, что бот является администратором канала
3. Проверьте, что канал является форум-группой (Topics enabled)
4. Проверьте логи на наличие ошибок

**Проблема**: Все сигналы идут в одну тему

**Решение**:
1. Проверьте, что `total_usd` вычисляется правильно (проверьте логи)
2. Убедитесь, что `SIZE_THRESHOLD_USD` установлен правильно
3. Проверьте, что оба Topic ID указаны в `.env`

## 📞 Поддержка

Если возникли проблемы, проверьте:
- Логи бота на наличие ошибок
- Правильность Topic ID через скрипты `get_topic_ids*.py`
- Настройки в `.env` файле

