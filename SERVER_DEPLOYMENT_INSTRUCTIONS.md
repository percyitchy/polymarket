# 🚀 Инструкции по развертыванию строгих критериев на сервере

## ✅ Что было сделано локально:

1. ✅ Добавлены новые поля в таблицу `wallets`:
   - `total_volume` - общий объём торговли
   - `roi` - доходность (ROI)
   - `avg_pnl_per_market` - средняя прибыль на рынок
   - `avg_stake` - средний размер ставки

2. ✅ Обновлены файлы:
   - `db.py` - добавлена поддержка новых полей
   - `wallet_analyzer.py` - передача новых метрик при анализе
   - `polymarket_notifier.py` - использование строгих критериев в мониторинге

3. ✅ Запущен переанализ: 17,202 кошелька переведены в статус 'pending'

4. ✅ Файлы загружены на сервер:
   - `db.py`
   - `wallet_analyzer.py`
   - `polymarket_notifier.py`
   - `reanalyze_completed_wallets.py`

## 📋 Следующие шаги на сервере:

### 1. Подключитесь к серверу:
```bash
ssh ubuntu@YOUR_SERVER_IP
```

### 2. Перейдите в директорию проекта:
```bash
cd /opt/polymarket-bot
```

### 3. Проверьте, что файлы загружены:
```bash
ls -lh db.py wallet_analyzer.py polymarket_notifier.py reanalyze_completed_wallets.py
```

### 4. Запустите переанализ кошельков (если нужно):
```bash
python3 reanalyze_completed_wallets.py
```

**Примечание:** Переанализ уже запущен локально, но если на сервере другая база данных, нужно запустить и там.

### 5. Обновите структуру базы данных:
База данных автоматически обновится при первом запуске, но можно проверить:
```bash
python3 -c "
from db import PolymarketDB
db = PolymarketDB('polymarket_notifier.db')
# Инициализация создаст новые поля автоматически
print('База данных обновлена')
"
```

### 6. Перезапустите сервисы:
```bash
sudo systemctl restart polymarket-bot.service
```

### 7. Проверьте статус:
```bash
sudo systemctl status polymarket-bot.service
```

### 8. Проверьте логи:
```bash
sudo journalctl -u polymarket-bot.service -f --lines=50
```

## 🎯 Строгие критерии фильтрации:

Теперь система использует следующие строгие критерии:

- **MIN_TRADES**: 6
- **WIN_RATE_THRESHOLD**: 65%
- **MINIMUM_MARKETS**: 12
- **MINIMUM_VOLUME**: $25,000
- **MINIMUM_ROI**: 0.25%
- **MINIMUM_AVG_PNL**: $50
- **MINIMUM_AVG_STAKE**: $100

## 📊 Мониторинг прогресса:

После перезапуска можно проверить прогресс:

```bash
python3 -c "
import sys
sys.path.insert(0, '.')
from db import PolymarketDB

db = PolymarketDB('polymarket_notifier.db')

# Статистика очереди
with db.get_connection() as conn:
    cursor = conn.cursor()
    cursor.execute('''
        SELECT 
            COUNT(*) as total,
            SUM(CASE WHEN status = 'pending' THEN 1 ELSE 0 END) as pending,
            SUM(CASE WHEN status = 'processing' THEN 1 ELSE 0 END) as processing,
            SUM(CASE WHEN status = 'completed' THEN 1 ELSE 0 END) as completed
        FROM wallet_analysis_jobs
    ''')
    row = cursor.fetchone()
    print(f'Ожидают анализа: {row[1] or 0:,}')
    print(f'В обработке: {row[2] or 0:,}')
    print(f'Завершено: {row[3] or 0:,}')

# Статистика кошельков
stats = db.get_wallet_stats()
print(f'Отслеживаемых (строгие критерии): {stats.get(\"tracked_wallets\", 0):,}')
"
```

## ⚠️ Важные замечания:

1. **База данных**: Убедитесь, что на сервере используется правильная база данных
2. **Переанализ**: Workers будут автоматически переанализировать кошельки из очереди
3. **Время**: Переанализ 17,000+ кошельков может занять несколько часов
4. **Мониторинг**: Следите за логами, чтобы убедиться, что всё работает корректно

## 🔧 Откат изменений (если нужно):

Если что-то пошло не так, можно откатить изменения:

```bash
# На сервере
cd /opt/polymarket-bot
git checkout HEAD -- db.py wallet_analyzer.py polymarket_notifier.py
sudo systemctl restart polymarket-bot.service
```

---

**Дата развертывания:** $(date)
**Версия:** Строгие критерии фильтрации v1.0

