# HashiDive Trader Explorer Scraper

## 📋 Описание

Скрипт для парсинга кошельков со страницы [HashiDive Trader Explorer](https://hashdive.com/Trader_explorer).

## ✅ Что реализовано

1. **Скрипт парсинга** (`fetch_hashdive_trader_explorer.py`)
   - Использует Playwright для работы с JavaScript-рендерингом
   - Поддержка прокси из `.env`
   - Множественные стратегии извлечения адресов:
     - Из видимого текста страницы
     - Из HTML контента
     - Из таблиц/рядов
     - Из API ответов (перехват сетевых запросов)
     - Интерактивное взаимодействие (поиск, фильтры)
     - Infinite scroll для загрузки дополнительного контента
     - Пагинация (если доступна)

2. **Интеграция в основной пайплайн**
   - Функция `collect_wallets_from_hashdive_trader_explorer()` в `polymarket_notifier.py`
   - Интегрировано в `daily_wallet_analysis.py`
   - Добавлено в Source Breakdown отчеты

## 🚀 Использование

### Ручной запуск

```bash
python3 fetch_hashdive_trader_explorer.py
```

Скрипт:
- Откроет страницу HashiDive Trader Explorer
- Извлечет все найденные адреса кошельков
- Сохранит их в файл `hashdive_trader_explorer_wallets_YYYYMMDD_HHMMSS.txt`
- Создаст скриншот для отладки

### Автоматический сбор (в daily_wallet_analysis.py)

1. Добавьте в `.env`:
```bash
ENABLE_HASHDIVE_TRADER_EXPLORER=true
```

2. При запуске `daily_wallet_analysis.py` скрипт автоматически:
   - Соберет адреса с Trader Explorer
   - Добавит их в очередь анализа
   - Прогонит через фильтры (win rate, trades, daily frequency, activity)
   - Покажет статистику в Source Breakdown

## 📊 Текущие результаты

**Тестовый запуск:**
- Найдено адресов: 12
- Источник: статический HTML страницы

**Ограничения:**
- Страница использует JavaScript-рендеринг
- Данные могут загружаться динамически через API
- Может потребоваться интерактивное взаимодействие (поиск, фильтры)
- Infinite scroll может загружать больше адресов

## 🔧 Улучшения

Скрипт включает:
- ✅ Перехват сетевых запросов для поиска API endpoints
- ✅ Infinite scroll для загрузки дополнительного контента
- ✅ Поддержка пагинации
- ✅ Интерактивное взаимодействие (поиск, фильтры)
- ✅ Множественные стратегии извлечения адресов

## ⚠️ Важные замечания

1. **Требования:**
   - Playwright установлен (`pip install playwright && playwright install`)
   - Прокси настроены (опционально, из `.env`)

2. **Производительность:**
   - Скрипт может работать медленно (10-30 секунд на страницу)
   - Infinite scroll может занять дополнительное время

3. **Ограничения:**
   - Если страница требует авторизации, скрипт может не найти адреса
   - Если данные загружаются только через интерактивный поиск, нужно улучшить логику взаимодействия

## 📝 Следующие шаги

Если нужно получить больше адресов:

1. **Проверить скриншот** (`hashdive_trader_explorer_screenshot.png`) - понять структуру страницы
2. **Улучшить интерактивное взаимодействие:**
   - Попробовать ввести поисковый запрос
   - Кликнуть на фильтры/сортировку
   - Дождаться загрузки данных
3. **Найти API endpoint:**
   - Проверить перехваченные сетевые запросы в логах
   - Использовать найденный API endpoint напрямую (если доступен)

## 🔗 Интеграция

Скрипт интегрирован в:
- `polymarket_notifier.py` - функция `collect_wallets_from_hashdive_trader_explorer()`
- `daily_wallet_analysis.py` - автоматический сбор при `ENABLE_HASHDIVE_TRADER_EXPLORER=true`
- Source Breakdown отчеты - показывает статистику по источнику `hashdive_trader_explorer`

