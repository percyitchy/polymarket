# 📊 Polymarket Notifier Bot - Полная документация проекта

## 🎯 Описание проекта

**Polymarket Notifier Bot** — автоматизированная система мониторинга высокопроизводительных кошельков на Polymarket для обнаружения консенсусных сигналов. Бот отслеживает когда несколько успешных трейдеров одновременно покупают одинаковый исход рынка и отправляет уведомления в Telegram.

---

## 🔍 Критерии отбора кошельков для мониторинга

### Основные условия (строгие)

| Параметр | Значение | Описание |
|----------|----------|----------|
| **Минимум сделок** | **6+** | Минимум 6 закрытых позиций в истории |
| **Win Rate** | **75% - 100%** | Процент выигрышных рынков (от 75% до 100%) |
| **Максимум сделок** | **≤ 1000** | Не более 1000 закрытых позиций (исключает ботов/масс-трейдеров) |
| **Дневная активность** | **≤ 20 сделок/день** | Средняя частота торговли не более 20 сделок в сутки |

### Дополнительные настройки

| Параметр | Значение | Описание |
|----------|----------|----------|
| **MAX_WALLETS** | **2000** | Максимальное количество отслеживаемых кошельков одновременно |
| **API Workers** | **7** | Количество параллельных воркеров для анализа кошельков |

---

## 📈 Источники кошельков

Бот собирает кошельки из следующих источников:

1. **Polymarket Leaderboards** (через API и Playwright):
   - Daily profit leaderboard
   - Weekly profit leaderboard
   - Monthly profit leaderboard

2. **polymarketanalytics.com/traders** (основной источник):
   - API endpoint: `https://polymarketanalytics.com/api/traders-tag-performance`
   - Собирает до 2000 адресов за раз
   - Автоматическая проверка каждые 12 часов (cron)

3. **Система очереди анализа**:
   - Все найденные адреса добавляются в очередь `wallet_analysis_jobs`
   - 7 воркеров обрабатывают очередь параллельно
   - Fast-filter: кошельки с <6 trades сразу отклоняются без API запросов

---

## 🔔 Правила отправки сигналов (Alert Rules)

### Требования для сигнала

1. **Минимальный консенсус**: **3+ кошелька** должны купить одинаковый исход
2. **Временное окно**: Все сделки должны произойти в течение **15 минут** (ALERT_WINDOW_MIN)
3. **Направление**: Поддерживаются как BUY, так и SELL сигналы

### Фильтры (что блокирует сигналы)

#### 1. Завершенные/закрытые рынки
- **Цена >= 0.999** или **<= 0.001** → Рынок разрешен (resolved) → ❌ БЛОКИРУЕТСЯ
- **Цена >= 0.98** или **<= 0.02** → Рынок почти закрыт → ❌ БЛОКИРУЕТСЯ
- **Статус рынка**: Проверка через CLOB API (`closed`, `resolved`, `finished`) → ❌ БЛОКИРУЕТСЯ

#### 2. Дедупликация (предотвращение спама)

**30-минутное правило** (после первого алерта):
- Если пришел сигнал по тому же рынку и тому же исходу в течение 30 минут:
  - ❌ Блокируется, если количество кошельков не выросло
  - ✅ Разрешается, если:
    - Изменился исход (Yes → No или наоборот)
    - Количество кошельков выросло (3 → 4, 4 → 5)
    - Появился новый кошелек (не был в последних 3 сигналах)
    - Цена изменилась на ≥1%
    - Прошло >60 минут с последнего сигнала

**10-минутное правило** (legacy):
- Если в течение 10 минут, тот же исход, количество не выросло, цена не изменилась → ❌ Блокируется

#### 3. Проверка конфликтов
- Если недавно был сигнал с противоположной стороной (BUY vs SELL) → ❌ Блокируется

#### 4. Расхождение цен входа
Для первых 3 трейдеров:
- Если цена < $0.05: расхождение не ограничено
- Если цена $0.05 - $0.5: расхождение ≤ 25%
- Если цена ≥ $0.5: расхождение ≤ 10%
- Если расхождение больше → ❌ Блокируется

---

## 📱 Формат Telegram уведомлений

### Шаблон сигнала:

```
🔮 Alpha Signal Detected (X wallets)

🎯 Market: [Название рынка]
Total position: X,XXX USDC💰

🛒 Outcome: [Yes/No/Название исхода]
👤 Traders involved:

1. 0xXXX...XXX • WR: XX.X% (XX trades) @ $X.XXX
2. 0xXXX...XXX @ $X.XXX
3. 0xXXX...XXX • WR: XX.X% (XX trades) @ $X.XXX

Current price: $X.XXX

📅 YYYY-MM-DD HH:MM:SS UTC • Alert ID: XXXXXXXX

[View Market] (кнопка)
```

### Каналы Telegram:

- **Сигналы**: Отправляются в `TELEGRAM_CHAT_ID` с `message_thread_id` (topic support для форумов)
- **Технические уведомления**: Отправляются в `TELEGRAM_REPORTS_CHAT_ID` (стартапы, ошибки, статистика)

---

## 🗄️ База данных

### Таблицы:

1. **wallets** — Отслеживаемые кошельки
   - `address` (PRIMARY KEY)
   - `display` — Отображаемое имя
   - `traded_total` — Всего сделок
   - `win_rate` — Процент выигрыша
   - `realized_pnl_total` — Суммарный PnL
   - `daily_trading_frequency` — Частота торговли
   - `source` — Источник кошелька
   - `added_at`, `updated_at`

2. **last_trades** — Последние обработанные сделки
   - `address` (PRIMARY KEY)
   - `last_seen_trade_id`
   - `updated_at`

3. **alerts_sent** — История отправленных алертов (для дедупликации)
   - `alert_key` (PRIMARY KEY)
   - `condition_id`, `outcome_index`, `side`
   - `wallet_count`, `price`
   - `wallets_csv` — Список кошельков через запятую
   - `sent_at`

4. **rolling_buys** — Окна консенсуса
   - `k` — Ключ (hash от condition_id:outcome_index:side)
   - `data` — JSON с событиями и временными метками
   - `updated_at`

5. **wallet_analysis_jobs** — Очередь анализа кошельков
   - `id` (PRIMARY KEY)
   - `address`, `display`, `source`
   - `status` — pending/processing/completed/failed
   - `created_at`, `updated_at`, `next_retry_at`

6. **wallet_analysis_cache** — Кэш результатов анализа
   - `address` (PRIMARY KEY)
   - `traded_total`, `win_rate`, `realized_pnl_total`, `daily_trading_frequency`
   - `analysis_result` — accepted/rejected
   - `updated_at`

---

## ⚙️ Конфигурация (Environment Variables)

### Обязательные:
- `TELEGRAM_BOT_TOKEN` — Токен Telegram бота
- `TELEGRAM_CHAT_ID` — ID чата для сигналов
- `TELEGRAM_REPORTS_CHAT_ID` — ID чата для технических уведомлений (опционально)
- `TELEGRAM_TOPIC_ID` — ID топика в форум-группе (опционально, для topic support)

### Опциональные (значения по умолчанию):
- `POLL_INTERVAL_SEC=7` — Интервал проверки кошельков (секунды)
- `ALERT_WINDOW_MIN=15.0` — Временное окно для консенсуса (минуты)
- `MIN_CONSENSUS=3` — Минимум кошельков для сигнала
- `MAX_WALLETS=400` — Максимум отслеживаемых кошельков
- `MAX_PREDICTIONS=1000` — Максимум сделок на кошелек
- `ALERT_COOLDOWN_MIN=10.0` — Кулдаун между сигналами
- `ALERT_CONFLICT_WINDOW_MIN=10.0` — Окно для проверки конфликтов
- `API_MAX_WORKERS=7` — Количество воркеров для анализа
- `TELEGRAM_HEARTBEAT=0` — Включить heartbeat уведомления (0=выкл, 1=вкл)

---

## 🔄 Автоматизация

### Cron задачи:

1. **Проверка polymarketanalytics.com** (каждые 12 часов):
   ```bash
   0 */12 * * * cd /home/ubuntu/polymarket && python3 check_polymarket_analytics.py
   ```

2. **Обновление кошельков из лидербордов** (ежедневно в 00:20):
   ```bash
   20 0 * * * python3 refresh_wallets_from_leaderboards.py
   ```

3. **Экспорт в Excel** (ежедневно):
   ```bash
   0 1 * * * python3 export_wallets_to_excel.py
   ```

### Systemd сервис:

- **Сервис**: `polymarket-notifier.service`
- **Статус**: Запущен и работает 24/7
- **Автозапуск**: Включен
- **Перезапуск**: Автоматический при сбое

---

## 📊 Текущая статистика базы данных

- **Всего кошельков**: 1463
- **Среднее trades**: 702.1
- **Средний Win Rate**: 97.9%
- **Средняя daily frequency**: 14.84 сделок/день

### Распределение по источникам:
- **polymarket_analytics_periodic**: 1320 кошельков
- **polymarket_api**: 140 кошельков
- **leaderboard (today)**: 1 кошелек
- **Другие источники**: 2 кошелька

---

## 🛡️ Система фильтрации сигналов

### Многоуровневая проверка:

1. **В начале функции** (`is_market_active`):
   - Проверка статуса рынка через CLOB API
   - Проверка цены как индикатор завершения

2. **После получения цены**:
   - Проверка цены >= 0.999 или <= 0.001 → resolved
   - Проверка цены >= 0.98 или <= 0.02 → закрыт

3. **В notify.py перед отправкой**:
   - Финальная проверка цены
   - Блокировка, если цена = None

### Логирование подавленных сигналов:

Все блокировки логируются с причиной:
- `[SUPPRESS] resolved` — Рынок разрешен
- `[SUPPRESS] price_high` — Цена слишком высокая/низкая
- `[SUPPRESS] market_closed` — Рынок закрыт по статусу
- `[SUPPRESS] ignore_30m_same_outcome` — 30-минутное правило
- `[SUPPRESS] dedupe_no_growth_10m` — 10-минутное правило
- `[SUPPRESS] no_trigger_matched` — Нет триггеров для публикации
- `[SUPPRESS] opposite_recent` — Конфликт с противоположной стороной

---

## 📦 Основные компоненты

### 1. `polymarket_notifier.py` — Главный скрипт
- Мониторинг кошельков
- Обнаружение консенсуса
- Отправка алертов

### 2. `notify.py` — Telegram интеграция
- Форматирование сообщений
- Отправка в Telegram
- Получение информации о рынках

### 3. `db.py` — База данных
- Управление кошельками
- Хранение истории алертов
- Окна консенсуса

### 4. `wallet_analyzer.py` — Анализ кошельков
- Система очереди
- Воркеры для параллельной обработки
- Фильтрация по критериям

### 5. `fetch_polymarket_analytics_api.py` — API polymarketanalytics.com
- Получение списка трейдеров
- Парсинг JSON ответов

### 6. `check_polymarket_analytics.py` — Периодическая проверка
- Автоматическое добавление новых кошельков
- Проверка каждые 12 часов

---

## 🚀 Процесс работы

### 1. Инициализация
- Загрузка конфигурации из `.env`
- Инициализация базы данных
- Запуск воркеров анализа (7 штук)
- Отправка уведомления о старте

### 2. Сбор кошельков
- Парсинг лидербордов Polymarket
- Получение кошельков с polymarketanalytics.com
- Добавление в очередь анализа

### 3. Анализ кошельков
- Воркеры берут задачи из очереди
- Проверка через Polymarket Data API:
  - `/traded` → количество сделок
  - `/closed-positions` → win rate и PnL
  - Расчет daily frequency
- Сохранение подходящих кошельков в базу

### 4. Мониторинг
- Циклическая проверка всех кошельков (каждые 7 секунд)
- Получение новых сделок (BUY/SELL)
- Обновление окон консенсуса

### 5. Обнаружение консенсуса
- Группировка сделок по рынку/исходу/направлению
- Подсчет уникальных кошельков в окне (15 минут)
- Если >= 3 кошелька → проверка фильтров → отправка алерта

---

## 📝 Примечания

- Все времена в UTC
- База данных использует WAL mode для лучшей конкурентности
- Логи пишутся в `polymarket_notifier.log`
- Heartbeat уведомления выключены по умолчанию
- Topic ID поддерживается для Telegram форумов

---

## 🔧 Обслуживание

- **Ежедневная очистка**: Удаление кошельков, не соответствующих критериям
- **Еженедельное обновление**: Обновление списка кошельков из лидербордов
- **Автоматический мониторинг**: Проверка новых кошельков каждые 12 часов

---

*Последнее обновление: 2025-11-01*

