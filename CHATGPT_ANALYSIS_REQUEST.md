# Запрос для анализа проблемы фильтрации кошельков

## 🚨 Проблема

Из **2500+ кошельков** с `polymarketanalytics.com` и множества кошельков с Polymarket leaderboards (weekly/monthly, по 20 страниц) в базу сохраняется только **72 кошелька** после обработки **2625 jobs**.

**Ожидалось:** Сотни или тысячи кошельков должны соответствовать критериям  
**Реальность:** Только 72 кошелька (2.7% от обработанных)

## 📊 Текущая статистика

- **Всего jobs в очереди:** 2628
- **Completed jobs:** 2625
- **Failed jobs:** 3
- **Уникальных адресов:** 2628
- **Кошельков в базе wallets:** 72
- **Отслеживаемых:** 68-72

**Статистика по кошелькам в базе:**
- Все прошедшие критерии: 72 (100%)
- Отклонено (<6 trades): 0
- Отклонено (>=6 trades, но <75% win rate): 0

## 🔍 Критерии фильтрации

### В `wallet_analyzer.py` метод `_analyze_wallet()`:

1. **Fast filter (строка 235-242):**
   ```python
   if traded < 6:
       # Отбрасывается сразу
   ```

2. **Inactive filter (строка 256-264):**
   ```python
   if last_trade_dt < three_months_ago:
       # Отбрасывается (last_trade > 90 дней)
   ```

3. **Main criteria (строка 271-274, 282):**
   ```python
   if traded >= 6 and win_rate >= 0.75:
       # Сохраняется в базу
   else:
       # Отбрасывается
   ```

### Полные критерии:
- `traded_total >= 6`
- `win_rate >= 0.75` (75%)
- `last_trade_at` не старше 90 дней
- `daily_trading_frequency <= 20.0` (проверяется при получении tracked wallets)

## 📁 Файлы для анализа

### 1. `wallet_analyzer.py`
**Ключевые методы:**
- `_analyze_wallet()` - строки 199-302 (основная логика фильтрации)
- `_get_total_traded()` - строки 470-483 (получение количества trades)
- `_get_closed_positions()` - строки 485-496 (получение закрытых позиций)
- `_compute_win_rate_and_pnl()` - строки 571-586 (расчёт win rate)
- `_get_last_trade_timestamp()` - строки 498-531 (получение последней сделки)

### 2. `db.py`
**Ключевые методы:**
- `get_tracked_wallets()` - строки 229-254 (получение отслеживаемых кошельков)
- `upsert_wallet()` - сохранение кошелька в базу
- `add_wallet_to_queue()` - строки 574-592 (добавление в очередь)

### 3. `polymarket_notifier.py`
**Ключевые методы:**
- `analyze_and_filter_wallets()` - строки 461-478 (добавление кошельков в очередь)
- `collect_wallets_from_polymarket_analytics()` - строки 371-400
- `collect_wallets_from_leaderboards()` - строки 402-458

### 4. `daily_wallet_analysis.py`
- `collect_from_polymarket_analytics()` - строки 35-47
- `collect_from_leaderboards()` - строки 49-67

## ❓ Вопросы для анализа

1. **Почему из 2625 обработанных jobs только 72 кошелька в базе?**
   - Где теряются остальные 2553 кошелька?
   - На каком этапе они отбрасываются?

2. **Правильно ли работает получение данных из API?**
   - Корректно ли считается `traded_total` через `_get_total_traded()`?
   - Корректно ли считается `win_rate` через `_compute_win_rate_and_pnl()`?
   - Правильно ли получается `last_trade_at` через `_get_last_trade_timestamp()`?

3. **Соответствуют ли критерии реальности?**
   - Действительно ли только 2.7% кошельков имеют win_rate >= 75%?
   - Или проблема в логике расчёта win_rate?
   - Может быть, большинство кошельков имеют win_rate 60-74%?

4. **Есть ли баги в логике фильтрации?**
   - Правильно ли работает проверка `last_trade_at`?
   - Нет ли ошибок в условиях фильтрации?
   - Правильно ли обрабатываются ошибки API?

5. **Как можно улучшить логирование?**
   - Нужна статистика по причинам отклонения
   - Нужны логи для каждого этапа фильтрации

## 🔎 Возможные причины

### 1. Слишком строгие критерии win_rate
- **Текущее:** `win_rate >= 0.75` (75%)
- **Гипотеза:** Большинство кошельков имеют win rate 60-74%

### 2. Проблема с получением closed_positions
- Метод `_get_closed_positions()` получает только первые 500 позиций
- Если у кошелька больше 500 закрытых позиций, win_rate может считаться неправильно

### 3. Проблема с last_trade_at
- Фильтр по 3 месяцам может быть слишком строгим
- Метод может возвращать None для некоторых кошельков

### 4. Проблема с добавлением в очередь
- Дубликаты могут игнорироваться через `INSERT OR IGNORE`
- Кошельки могут не добавляться, если уже есть в очереди

### 5. Ошибки при анализе
- Ошибки API могут приводить к пропуску кошельков
- Ошибки могут не логироваться должным образом

## 📝 Запрос для ChatGPT

```
Проанализируй код фильтрации кошельков в системе Polymarket.

ПРОБЛЕМА:
Из 2500+ кошельков с polymarketanalytics.com и множества кошельков с 
Polymarket leaderboards только 72 кошелька сохраняются в базу после 
обработки 2625 jobs (2.7% прохождения фильтрации).

КРИТЕРИИ ФИЛЬТРАЦИИ:
1. traded_total >= 6
2. win_rate >= 0.75 (75%)
3. last_trade_at не старше 90 дней
4. daily_trading_frequency <= 20.0

ФАЙЛЫ ДЛЯ АНАЛИЗА:
- wallet_analyzer.py (метод _analyze_wallet, строки 199-302)
- db.py (метод get_tracked_wallets, строки 229-254)
- polymarket_notifier.py (метод analyze_and_filter_wallets, строки 461-478)

ВОПРОСЫ:
1. Почему так мало кошельков проходит фильтрацию (только 2.7%)?
2. Правильно ли работает логика расчёта win_rate и traded_total?
3. Не слишком ли строгие критерии (особенно win_rate >= 0.75)?
4. Есть ли баги в логике фильтрации?
5. Как можно улучшить логирование для диагностики?

Пожалуйста, проанализируй код и найди причину проблемы.
```

## 📋 Дополнительная информация

**Источники кошельков:**
1. `polymarketanalytics.com` - до 2500 кошельков через API
2. `polymarket.com/leaderboard/overall/weekly/profit` - 20 страниц
3. `polymarket.com/leaderboard/overall/monthly/profit` - 20 страниц

**Процесс обработки:**
1. Кошельки собираются из источников
2. Добавляются в очередь `wallet_analysis_jobs`
3. Workers анализируют каждый кошелёк
4. Применяются критерии фильтрации
5. Прошедшие критерии сохраняются в таблицу `wallets`

**Текущее состояние:**
- Очередь полностью обработана (2625 completed, 3 failed)
- В базе только 72 кошелька
- Все 72 кошелька соответствуют критериям

**Проблема:** Куда делись остальные 2553 кошелька?

