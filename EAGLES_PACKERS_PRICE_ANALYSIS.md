# 🔍 Анализ проблемы с получением цены для Eagles vs. Packers

## 📋 Проблема

Suppressed alert пришел с "Цена недоступна" для рынка Eagles vs. Packers, хотя:
- Есть wallet_prices: $0.510 и $0.720
- Подключено 5 разных API источников
- Рынок был активен (Final)

## 🔍 Результаты тестирования

### Тест получения цены (локально):
```
Condition ID: 0x23e6e6f8a327a41bad1282fdc34e846f52e73e390d44b004ac92a329766e2848
Outcome Index: 0
Wallet Prices: {'0xa946...517a': 0.51, '0xf6c0...722e': 0.72}

Результат: 0.999 (из истории сделок)
```

**Вывод**: Цена получается успешно из истории сделок (шаг 3/5).

## 🔍 Возможные причины проблемы

### 1. **Проблемы с API в момент отправки алерта**
   - В момент отправки suppressed alert (05:00:02 UTC) все API могли быть недоступны
   - `http_get` возвращал `None` из-за проблем с прокси/сетью
   - История сделок не возвращала данные (рынок уже был закрыт - Final)

### 2. **wallet_prices не использовались как fallback**
   - `wallet_prices` должны были использоваться на шаге 5/5 в `price_fetcher`
   - Но если `price_fetcher` возвращал None до шага 5, то fallback не срабатывал
   - Или `wallet_prices` не были переданы в `price_fetcher`

### 3. **Проблема с формированием wallet_prices**
   - `wallet_prices` формируются из событий в `window_data`
   - Если события не содержат `price` или `wallet`, то `wallet_prices` остается пустым
   - В suppressed alert видно wallet_prices ($0.510 и $0.720), значит они были доступны

## ✅ Исправления

### 1. **Улучшено логирование в `price_fetcher.py`**
   - Добавлено подробное логирование для шага 5/5 (wallet_prices fallback)
   - Логируется содержимое `wallet_prices`, валидные цены, и причины ошибок
   - Добавлены traceback для исключений

### 2. **Улучшено логирование в `polymarket_notifier.py`**
   - Добавлено логирование процесса извлечения `wallet_prices` из событий
   - Логируется количество событий, количество извлеченных цен
   - Логируется содержимое `wallet_prices` перед передачей в `_get_current_price`

### 3. **Улучшено логирование в legacy методах**
   - Добавлено подробное логирование для Step 3 (wallet_prices fallback в legacy методах)
   - Логируется содержимое `wallet_prices` и причины ошибок

## 📊 Порядок источников цен

1. **Polymarket CLOB API /price** (требует авторизацию - не настроена)
2. **HashiDive API** (лимит исчерпан - 429)
3. **История сделок** (data-api + clob) ← **УСПЕШНО** (0.999)
4. **FinFeed API**
5. **Средняя цена из wallet_prices** ← **ДОЛЖЕН СРАБОТАТЬ**, если все API недоступны

## 🎯 Выводы

1. **Цена получается** из истории сделок (0.999) - рынок был разрешен (Final)
2. **В момент отправки suppressed alert** цена была None, вероятно из-за:
   - Проблем с сетью/прокси (http_get возвращал None)
   - Рынок был уже закрыт, и история сделок не возвращала актуальные данные
3. **wallet_prices fallback должен был сработать**, но не сработал по неизвестной причине
4. **С улучшенным логированием** теперь будет видно, почему wallet_prices не использовались

## 📝 Рекомендации

1. **Проверить логи на сервере** после следующего suppressed alert с "Цена недоступна"
2. **Убедиться, что wallet_prices передаются** в `price_fetcher.get_current_price()`
3. **Проверить, что wallet_prices не пустые** перед вызовом `_get_current_price`
4. **Рассмотреть использование wallet_prices как приоритетного источника** для активных рынков

## 🔧 Следующие шаги

1. Мониторить логи на сервере для следующего suppressed alert
2. Проверить, что wallet_prices правильно извлекаются из событий
3. Убедиться, что wallet_prices fallback срабатывает, когда все API недоступны

