# 🔍 Диагностика: Почему нет сигналов за 10 часов

## 📊 Текущее состояние

**Дата проверки**: 2025-11-11 20:40 UTC

### Статус сервиса
- ✅ Сервис работает (active running, 8+ часов)
- ✅ Мониторинг активен: "Monitoring 69 tracked wallets"

### База данных
- **Всего кошельков**: 96
- **Отслеживаемых (is_tracked=1)**: 1 (проблема!)
- **Динамически отслеживаемых** (через `get_tracked_wallets()`): 69

### Консенсусы
- **MIN_CONSENSUS**: 3 (из .env)
- **Rolling windows с >=2 кошельками**: 2 (старые, 8:34 и 2:49)
- **Rolling windows с >=3 кошельками**: 0 (нет консенсусов!)

---

## 🚨 Основные проблемы

### 1. **Прокси падают → API возвращает None**

**Симптомы**:
- В логах: `"Proxy connection failed, retrying without proxy"`
- `http_get()` возвращает `None` для всех запросов
- Прямой запрос к API работает (Status: 200, Got 7 trades)

**Причина**:
- Прокси `socks5://pkg-private2-pool-onlyipv4-country-de-session-...` не работают
- Fallback на прямое подключение не всегда срабатывает
- `http_get()` может возвращать `None` при ошибках прокси

**Логи**:
```
2025-11-11 20:16:28 - Proxy connection failed, retrying without proxy
2025-11-11 20:30:39 - Proxy connection failed, retrying without proxy
[Trades] HTTP call returned None for address=... side=BUY, trying HashiDive fallback
```

---

### 2. **MIN_CONSENSUS=3, но консенсусов только 2 кошелька**

**Проблема**:
- В `rolling_buys` есть окна с 2 кошельками
- Но `MIN_CONSENSUS=3`, поэтому консенсус не достигается
- Нужно либо снизить `MIN_CONSENSUS` до 2, либо дождаться 3+ кошельков

**Статистика**:
- Rolling windows с >=2 кошельками: 2 (старые)
- Rolling windows с >=3 кошельками: 0

---

### 3. **Нет новых событий**

**Причина**:
- API не возвращает сделки из-за проблем с прокси
- HashiDive fallback тоже возвращает 0 trades
- Новые события не добавляются в `rolling_buys`

**Последнее обновление rolling_buys**: 2025-11-11T18:40:29 (2 часа назад)

---

## 🔧 Решения

### Решение 1: Исправить проблему с прокси

**Вариант A**: Отключить прокси временно
```bash
# В .env закомментировать или удалить:
# POLYMARKET_PROXIES=...
```

**Вариант B**: Исправить fallback в `http_get()`
- Убедиться, что при ошибке прокси всегда пробуется прямое подключение
- Логировать успешные fallback'и

### Решение 2: Снизить MIN_CONSENSUS до 2

Если хотите получать сигналы от 2 кошельков:
```bash
# В .env:
MIN_CONSENSUS=2
```

**⚠️ Внимание**: Это увеличит количество сигналов, но может быть больше ложных срабатываний.

### Решение 3: Проверить HashiDive fallback

HashiDive тоже возвращает 0 trades. Нужно проверить:
- Работает ли HashiDive API
- Правильно ли парсятся ответы
- Есть ли кредиты на HashiDive API

---

## 📋 Рекомендации

1. **Немедленно**: Отключить прокси или исправить fallback
2. **Проверить**: Работает ли HashiDive API (кредиты, ключ)
3. **Рассмотреть**: Снизить `MIN_CONSENSUS` до 2 для тестирования
4. **Мониторинг**: Добавить логирование успешных fallback'ов

---

## 🔍 Дополнительная диагностика

### Проверить работу API без прокси:
```bash
cd /opt/polymarket-bot
python3 -c "
import requests
url = 'https://data-api.polymarket.com/trades'
params = {'user': '0x5b457c828fbd8bb078', 'side': 'BUY', 'limit': 10}
response = requests.get(url, params=params, timeout=10)
print(f'Status: {response.status_code}, Trades: {len(response.json())}')
"
```

### Проверить HashiDive:
```bash
# Проверить кредиты и работу API
```

---

## ✅ Ожидаемый результат после исправлений

1. API возвращает сделки (без прокси или с исправленным fallback)
2. Новые события добавляются в `rolling_buys`
3. Консенсусы >= MIN_CONSENSUS формируются
4. Сигналы отправляются в Telegram



