# Чеклист проверки работы MCP серверов

## ✅ Проверка конфигурации

- [x] Файл `~/.cursor/mcp.json` существует
- [x] JSON синтаксис корректен (проверено)
- [x] Все три сервера настроены:
  - [x] Task Master AI
  - [x] Context7 (с API ключом)
  - [x] Tavily (с API ключом)

## 🔄 Шаг 1: Перезапуск Cursor

**ВАЖНО:** После изменения `mcp.json` обязательно перезапустите Cursor полностью:
1. Закройте все окна Cursor
2. Запустите Cursor заново
3. Дождитесь полной загрузки

## 🧪 Шаг 2: Проверка работы

### Проверка 1: Task Master AI
В чате Cursor спросите:
```
"Помоги мне инициализировать Task Master в этом проекте"
```
или
```
"Можешь разбить этот проект на задачи?"
```

**Ожидаемый результат:** AI должен использовать инструменты Task Master для работы с задачами.

### Проверка 2: Tavily (поиск в интернете)
В чате Cursor спросите:
```
"Найди информацию о последних обновлениях Python 3.12"
```
или
```
"Какие лучшие практики для TypeScript в 2025 году?"
```

**Ожидаемый результат:** AI должен использовать Tavily для поиска актуальной информации в интернете.

### Проверка 3: Context7 (документация)
Context7 работает автоматически в фоне. Попробуйте:
```
"Помоги мне использовать React Query для работы с API"
```

**Ожидаемый результат:** AI должен использовать актуальную документацию из Context7.

## 🔍 Альтернативная проверка через настройки Cursor

1. Откройте Cursor Settings (Ctrl+Shift+J или Cmd+,)
2. Перейдите на вкладку **MCP**
3. Должны быть видны три сервера:
   - task-master-ai
   - context7
   - tavily
4. Проверьте статус каждого сервера (должен быть активен)

## ⚠️ Возможные проблемы

### Проблема: Серверы не загружаются
**Решение:**
- Убедитесь, что перезапустили Cursor полностью
- Проверьте, что Node.js установлен: `node --version`
- Проверьте, что npx доступен: `npx --version`

### Проблема: Ошибки с API ключами
**Решение:**
- Проверьте правильность ключей в `~/.cursor/mcp.json`
- Убедитесь, что ключи не истекли
- Для Tavily проверьте лимиты на https://tavily.com/
- Для Context7 проверьте статус на https://smithery.ai/

### Проблема: Task Master не отвечает
**Решение:**
- Task Master может работать без ключей (базовая функциональность)
- Если нужна расширенная функциональность, добавьте API ключи
- Попробуйте явно попросить: "Используй Task Master для..."

## 📝 Логи для диагностики

Если что-то не работает, проверьте логи Cursor:
- macOS: `~/Library/Logs/Cursor/`
- Windows: `%APPDATA%\Cursor\logs\`

Ищите ошибки связанные с MCP или конкретными серверами.

## ✅ Итоговая проверка

После перезапуска Cursor и тестирования, все должно работать автоматически. Вы не должны явно указывать, какой инструмент использовать - Cursor сам выберет нужный.

