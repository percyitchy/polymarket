# 🔧 Обновление минимального размера позиции до $2000

## Проблема
Сигналы с позицией меньше $2000 всё ещё проходят, хотя в коде установлено значение по умолчанию $2000.

## Причина
На сервере в файле `.env` **отсутствует** переменная `MIN_TOTAL_POSITION_USD`, поэтому используется значение по умолчанию из кода ($2000), но возможно сервис не был перезапущен после изменений.

## Решение

### Вариант 1: Автоматическое добавление (рекомендуется)

1. **Загрузите скрипт на сервер:**
```bash
# Локально
scp add_min_position_to_env.sh ubuntu@YOUR_SERVER_IP:/tmp/
```

2. **Подключитесь к серверу и выполните:**
```bash
ssh -l ubuntu YOUR_SERVER_IP
cd /opt/polymarket-bot
chmod +x /tmp/add_min_position_to_env.sh
sudo /tmp/add_min_position_to_env.sh
```

3. **Перезапустите сервис:**
```bash
sudo systemctl restart polymarket-bot
```

4. **Проверьте логи:**
```bash
sudo journalctl -u polymarket-bot -n 50 | grep MIN_TOTAL_POSITION_USD
# Должно быть: [Config] MIN_TOTAL_POSITION_USD=$2000
```

### Вариант 2: Ручное добавление

1. **Подключитесь к серверу:**
```bash
ssh -l ubuntu YOUR_SERVER_IP
cd /opt/polymarket-bot
```

2. **Откройте файл .env:**
```bash
nano .env
```

3. **Найдите секцию "# Monitoring Configuration"** и добавьте строку после `MIN_CONSENSUS=`:

```env
# Monitoring Configuration
POLL_INTERVAL_SEC=7
ALERT_WINDOW_MIN=15
MIN_CONSENSUS=2
MIN_TOTAL_POSITION_USD=1000            # Minimum total position size in USDC to send alert
MAX_WALLETS=9200
MAX_PREDICTIONS=1900
```

4. **Сохраните файл:**
   - Нажмите `Ctrl+O` (сохранить)
   - Нажмите `Enter` (подтвердить)
   - Нажмите `Ctrl+X` (выйти)

5. **Перезапустите сервис:**
```bash
sudo systemctl restart polymarket-bot
```

6. **Проверьте, что значение загружено:**
```bash
sudo journalctl -u polymarket-bot -n 100 | grep MIN_TOTAL_POSITION_USD
```

### Вариант 3: Быстрое добавление через sed (Linux)

```bash
ssh -l ubuntu YOUR_SERVER_IP
cd /opt/polymarket-bot

# Создайте резервную копию
cp .env .env.backup

# Добавьте переменную после MIN_CONSENSUS
sed -i '/^MIN_CONSENSUS=/a MIN_TOTAL_POSITION_USD=2000            # Minimum total position size in USDC to send alert' .env

# Проверьте
grep MIN_TOTAL_POSITION_USD .env

# Перезапустите сервис
sudo systemctl restart polymarket-bot
```

## Проверка

После перезапуска проверьте логи:

```bash
# Проверка конфигурации при запуске
sudo journalctl -u polymarket-bot -n 100 | grep "MIN_TOTAL_POSITION_USD"

# Должно быть видно:
# [Config] MIN_CONSENSUS=2, MAX_WALLETS=9200, ALERT_WINDOW_MIN=15.0, ALERT_COOLDOWN_MIN=30.0 minutes, MIN_TOTAL_POSITION_USD=$2000
```

При попытке отправить сигнал с позицией < $2000 должно быть:
```
[CONSENSUS] ⏭️  BLOCKED: Insufficient total position size - $XXX.XX < $2000.00
```

## Текущая конфигурация в коде

- **Значение по умолчанию**: `2000.0` (строка 125 в `polymarket_notifier.py`)
- **Проверка**: Выполняется на Step 10/10 перед отправкой сигнала (строка 2309)
- **Логирование**: Значение логируется при запуске (строка 134)

## Важно

После изменения `.env` файла **обязательно перезапустите сервис**, иначе изменения не применятся!

```bash
sudo systemctl restart polymarket-bot
```

## Проверка локально

Для проверки конфигурации локально используйте:

```bash
python3 check_min_position_size.py
```
