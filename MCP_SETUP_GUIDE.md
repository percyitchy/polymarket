# Руководство по установке и использованию MCP серверов в Cursor

## 📋 Что это за инструменты?

### 1. **Task Master AI** (`claude-task-master`)
**Назначение:** Система управления задачами с поддержкой ИИ
- Автоматически разбивает проекты на задачи
- Управляет задачами и подзадачами
- Планирует следующий шаг разработки
- Исследует актуальную информацию для проекта
- Работает с PRD (Product Requirements Document)

**⚠️ Важно: Два способа установки**

Task Master AI можно использовать двумя способами:

1. **Как MCP сервер** (рекомендуется для Cursor):
   - Интегрируется напрямую в чат Cursor
   - Используется через конфигурацию `~/.cursor/mcp.json`
   - Работает автоматически в AI-чате
   - ✅ **Этот способ уже настроен**

2. **Как CLI инструмент** (через консоль):
   - Устанавливается: `npm install -g task-master-ai`
   - Используется через команды в терминале: `task-master init`, `task-master list` и т.д.
   - Работает независимо от Cursor

**Для Cursor рекомендуется использовать MCP сервер** - он интегрирован в чат и работает автоматически.

**Когда использовать:**
- При начале нового проекта - создайте PRD и попросите разбить на задачи
- Когда нужно понять, что делать дальше - спросите "Что следующая задача?"
- Для исследования - "Исследуй лучшие практики для JWT аутентификации"
- НЕ нужно явно указывать Cursor - он сам использует инструменты когда нужно

### 2. **Context7** (`upstash/context7`)
**Назначение:** Актуальная документация для LLM и AI-редакторов
- Предоставляет актуальную документацию по фреймворкам и библиотекам
- Помогает AI понимать контекст вашего кода
- Автоматически подтягивает релевантную документацию

**Когда использовать:**
- Работает автоматически в фоне
- Cursor сам использует его для понимания контекста вашего кода
- НЕ нужно явно указывать - инструмент работает прозрачно

### 3. **Tavily MCP** (`tavily-ai/tavily-mcp`)
**Назначение:** Поиск в интернете в реальном времени для AI
- Поиск актуальной информации в интернете
- Извлечение данных с веб-сайтов
- Создание карт сайтов
- Получение свежих данных для AI-ассистента

**Когда использовать:**
- Когда нужна актуальная информация из интернета
- При работе с новыми технологиями или библиотеками
- Для получения свежих данных о проектах/сервисах
- Cursor автоматически использует его когда нужен поиск в интернете

## 🚀 Установка

### Шаг 1: Конфигурация уже создана
Файл `~/.cursor/mcp.json` уже настроен с тремя серверами.

### Шаг 2: Получение API ключей

#### Task Master AI
**Способ установки:** Уже настроен как **MCP сервер** в `~/.cursor/mcp.json`

**Нужны ли ключи:** Опционально, но рекомендуется для расширенной функциональности
- **Anthropic API Key** (для Claude): https://console.anthropic.com/
- **OpenAI API Key**: https://platform.openai.com/api-keys
- **Perplexity API Key**: https://www.perplexity.ai/settings/api
- И другие по необходимости

**Бесплатные опции:**
- Можно использовать без ключей (ограниченная функциональность)
- Или использовать Claude Code (не требует ключей)

**Альтернатива - CLI установка (опционально):**
Если хотите использовать Task Master также через консоль:
```bash
npm install -g task-master-ai
# Затем используйте команды: task-master init, task-master list и т.д.
```
Но для работы в Cursor это не обязательно - MCP сервер уже настроен.

#### Context7
**Нужен ли ключ:** ДА, обязателен
- **Smithery Key**: https://smithery.ai/
  1. Зарегистрируйтесь на smithery.ai
  2. Получите ваш API ключ
  3. Вставьте в `SMITHERY_KEY` в конфигурации

**Стоимость:** Проверьте на smithery.ai - обычно есть бесплатный тариф

**✅ Статус:** Ключ уже добавлен в конфигурацию

#### Tavily
**Нужен ли ключ:** ДА, обязателен
- **Tavily API Key**: https://tavily.com/
  1. Зарегистрируйтесь на tavily.com
  2. Получите API ключ
  3. Вставьте в `TAVILY_API_KEY` в конфигурации

**Стоимость:** 
- Есть бесплатный тариф (ограниченное количество запросов)
- Платные планы для большего использования

**✅ Статус:** Ключ уже добавлен в конфигурацию

### Шаг 3: Добавление ключей в конфигурацию

✅ **Ключи уже добавлены!**
- ✅ `SMITHERY_KEY` - добавлен
- ✅ `TAVILY_API_KEY` - добавлен
- ⚠️ Ключи для Task Master опциональны (можно добавить позже при необходимости)

### Шаг 4: Перезапуск Cursor
После добавления ключей перезапустите Cursor, чтобы изменения вступили в силу.

## 💡 Как использовать

### Нужно ли говорить Cursor когда использовать?

**Короткий ответ: НЕТ, не нужно!**

Все три инструмента работают автоматически:
- **Task Master** - Cursor сам использует его инструменты когда вы просите помочь с задачами
- **Context7** - работает в фоне, автоматически подтягивает документацию
- **Tavily** - Cursor автоматически использует его для поиска в интернете когда нужно

### Примеры использования

#### Task Master:
```
"Можешь помочь мне разбить этот проект на задачи?"
"Что следующая задача для работы?"
"Исследуй лучшие практики для React Query v5"
"Помоги реализовать задачу 3"
```

#### Context7:
Работает автоматически - просто пишите код, и Cursor будет использовать актуальную документацию.

#### Tavily:
```
"Найди информацию о последних обновлениях Next.js 15"
"Какие лучшие практики для TypeScript в 2025?"
```

## 💰 Пополнение баланса

### Task Master AI
- **Бесплатно**: Базовая функциональность без API ключей
- **Платно**: Если используете API ключи (Anthropic, OpenAI и т.д.)
  - Зависит от провайдера API
  - Anthropic: ~$3-15 за миллион токенов
  - OpenAI: ~$0.50-30 за миллион токенов (зависит от модели)

### Context7
- Проверьте тарифы на https://smithery.ai/
- Обычно есть бесплатный тариф с ограничениями

### Tavily
- **Бесплатный тариф**: Ограниченное количество запросов
- **Платные планы**: От ~$20/месяц для большего использования
- Проверьте актуальные цены на https://tavily.com/pricing

## ✅ Проверка установки

После перезапуска Cursor вы можете проверить установку:

1. Откройте чат с AI в Cursor
2. Спросите: "Какие MCP инструменты доступны?"
3. Или попробуйте использовать функциональность:
   - "Помоги мне разбить проект на задачи" (Task Master)
   - "Найди информацию о..." (Tavily)

## 🔧 Оптимизация Task Master (опционально)

Если хотите уменьшить использование токенов, можете настроить режим загрузки инструментов:

Добавьте в конфигурацию Task Master:
```json
"env": {
  "TASK_MASTER_TOOLS": "standard",  // или "core" для минимума
  ...
}
```

Режимы:
- `all` (по умолчанию) - все 36 инструментов (~21,000 токенов)
- `standard` - 15 инструментов (~10,000 токенов)
- `core` или `lean` - 7 инструментов (~5,000 токенов)

## 📝 Примечания

1. **Безопасность**: Никогда не коммитьте файл `mcp.json` с реальными ключами в Git!
2. **Перезапуск**: После изменения конфигурации всегда перезапускайте Cursor
3. **Проверка ключей**: Убедитесь, что ключи действительны перед использованием
4. **Лимиты**: Следите за использованием API, чтобы не превысить лимиты бесплатного тарифа

